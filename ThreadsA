// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
package problem2;

public class ThreadA implements Runnable {
   private final SharedState s;

   public ThreadA(SharedState s) {
      this.s = s;
   }

   public void run() {
      try {
         this.s.A1 = (int)SumUtil.sum(500);
         this.s.sigA1Done.release();
         this.s.sigB2Done.acquire();
         this.s.A2 = this.s.B2 + (int)SumUtil.sum(300);
         this.s.sigA2Done.release();
         this.s.sigB3Done.acquire();
         this.s.A3 = this.s.B3 + (int)SumUtil.sum(400);
      } catch (InterruptedException var2) {
         Thread.currentThread().interrupt();
      }

   }
}
